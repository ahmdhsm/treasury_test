// @protected is an annotation (and not a language keyword) provide from package:meta
// and that is used by dartanalyzer.
// It's used to provide a hint when members are used outside of subclasses, by the Dart Analyzer.
// before using @protected, we must install package:meta first
@protected
void main() {}
